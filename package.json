{
  "name": "ecommerce-api",
  "version": "1.0.0",
  "description": "A complete RESTful API for a small e-commerce platform",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "seed": "node -e \"require('dotenv').config(); require('./src/models'); const seed = require('./src/seed'); const db = require('./src/config/db'); db.sync().then(() => seed()).then(() => db.close())\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "jsonwebtoken": "^9.0.0",
    "sequelize": "^6.37.8",
    "sqlite3": "^6.0.1",
    "swagger-jsdoc": "^6.2.0",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
